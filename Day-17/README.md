Developed and analyzed an E-commerce database using customers, orders, and payments tables.

Applied:

INNER JOIN and LEFT JOIN for relational data analysis
NULL handling to identify data inconsistencies
Aggregate functions (SUM, MAX) to derive key metrics
GROUP BY and HAVING for advanced filtering
ORDER BY and LIMIT to identify top-performing customers

All SQL queries are documented in the project file.
